package files

//The files package handles the file layer of the system.
//The file layer are stuff like finding files, writing to files and
//knowing which files should be backed up. To run the tests of this package you have
//run the make test command since the tests require a prior setup to work.
